name: nf4-do-01
on: workflow_dispatch

jobs:
  nf4-do-01:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: create_branche
        run: |
          git branch testing